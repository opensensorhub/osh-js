const VERSION = typeof "2.3.8" !== 'undefined' ? "2.3.8" : 'latest';
export const NullLoader = {
  id: 'image',
  name: 'Images',
  version: VERSION,
  mimeTypes: ['application/x.empty'],
  extensions: ['null'],
  parse: async (arrayBuffer, options) => arrayBuffer,
  parseSync: (arrayBuffer, options) => arrayBuffer,
  parseInBatches: async (asyncIterator, options) => async function* parseInBatches() {
    yield* asyncIterator;
  }(),
  tests: [() => false],
  options: {}
};
//# sourceMappingURL=null-loader.js.map